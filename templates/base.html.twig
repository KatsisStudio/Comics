<!DOCTYPE html>
<html lang="en">
    <head>
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta charset="UTF-8">
        {% if metadata is null %}
        <title>Katsis Comics</title>
        <meta property="og:title" content="Katsis Comics"/>
        <meta property="og:image" content="https://katsis.net/img/logo.png">
        {% else %}
        <title>{{ metadata.metadata.name }}</title>
        <meta property="og:title" content="{{ metadata.metadata.name }}"/>
        <meta property="og:image" content="https://blog.katsis.net/{{ metadata.metadata.preview }}">
        <meta property="og:description" content="{{ metadata.metadata.description }}"/>
        <meta name="description" content="{{ metadata.metadata.description }}">
        {% endif %}
        <meta name="theme-color" content="#ff7f7f">

        <link rel="stylesheet" type="text/css" href="css/index.css"/>
    </head>
    <body>
    {% block content %}{% endblock %}
    </body>
</html>