<!DOCTYPE html>
<html lang="en">
    <head>
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta charset="UTF-8">
        {% if metadata is null %}
        <title>Katsis Comics</title>
        <meta property="og:title" content="Katsis Comics"/>
        <meta property="og:image" content="https://katsis.net/img/logo.png">
        {% else %}
        <title>{{ metadata.name }}</title>
        <meta property="og:title" content="{{ metadata.name }}"/>
        <meta property="og:image" content="https://comic.katsis.net/{{ metadata.preview }}">
        <meta property="og:description" content="{{ metadata.description }}"/>
        <meta name="description" content="{{ metadata.description }}">
        <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,0,0" />
        {% endif %}
        <meta name="theme-color" content="#ff7f7f">

        <link rel="stylesheet" type="text/css" href="css/index.css"/>
        <script src="js/loader.js"></script>
    </head>
    <body>
    {% block content %}{% endblock %}
    </body>
</html>